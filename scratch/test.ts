import knex from 'knex'; import config from '../src/db/knexfile'; const db = knex(config.development); async function run() { try { await db('vehicles').insert({ name: 'Test', plate_number: 'TEST-123', category: 'car', daily_rate: 100 }); console.log('First insert ok'); await db('vehicles').insert({ name: 'Test', plate_number: 'TEST-123', category: 'car', daily_rate: 100 }); console.log('Second insert ok'); } catch (err: any) { console.log('ERROR THROWN:'); console.log('code:', err.code); console.log('message:', err.message); } finally { db.destroy(); } } run();
